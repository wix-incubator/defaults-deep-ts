language: node_js
node_js:
  - 8

sudo: false

install:
  - npm install

script:
  - npm run test

branches:
  only:
    - master

deploy:
  provider: npm
  skip_cleanup: true
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
